{
  "buildCommand": "npm run build",
  "outputDirectory": "/build",
  "build": {
    "env": {
      "NODEJS_AWS_HANDLER_NAME": "handler"
    }
  },
  "rewrites": [
    {
      "source": "/api/(.*)", 
      "destination": "src/api"
    }
  ]
}